package com.panlijun.strategy;

/**
 * upflow router impl
 * @author panlijun
 */
public class UpRouterImpl implements Router{
    @Override
    public String route() {
        return "/upflow";
    }
}
